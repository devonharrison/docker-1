#to start debian shell
#docker run -it debian
#-i to get stdin stream -t to have a terminal driver added
#use these flags to interact with the debian shell else it would
#exit immediately
#run next 5 commands in debian
apt-get update
apt-get install build-essentials
apt-get install gcc
apt-get install git
